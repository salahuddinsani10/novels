const Author = require('./Author');
const Book = require('./Book');
// Commenting out the User model import as it is not required
// const User = require('./User');
const Review = require('./Review');

module.exports = {
  Author,
  Book,
  // User,
  Review
};
